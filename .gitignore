# ----------------------------------------------------------------------------
# Log data in and out — never committed. These hold real conversation content
# (prompts, names, paths, keys, ...); keep them local.
# ----------------------------------------------------------------------------
raw-logs/
clean-logs/

# OS / editor noise
.DS_Store
**/.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.mypy_cache/
.pytest_cache/

# Local tooling / settings
.claude/
